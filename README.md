# Kali Hiring Challenge 


Use this commands to install all the dependencies.

You will need `yarn`, `node >= 16` and `xcodebuild`

```
yarn

cd ios && pod install
```

And then run the app on XCode opening `ios/ChallengerMovies.xcworkspace`